apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gateway-portal
  annotations:
    kubernetes.io/ingress.class: "istio"
spec:
  rules:
  - http:
      paths:
      - path: /portal/.*
        backend:
          serviceName: portal
          servicePort: 8080